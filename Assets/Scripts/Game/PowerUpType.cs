using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum PowerupType { COIN, AMMO, SLOW }
public class PowerUpType : MonoBehaviour
{
    public PowerupType state;
}
